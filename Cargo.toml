[package]
name = "bevy_doors"
version = "0.1.0"
edition = "2021"
description = "A minimal Doors-inspired horror hallway game for macOS (Bevy)"

[dependencies]
bevy = { version = "0.18", features = ["debug"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
